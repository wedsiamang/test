# スコープ
#### ローカル
- 同名の変数がある場合は必ず一番内側のローカル変数が呼ばれる。
#### インスタンスフィールド変数
- 非staticメソッドから、thisを使えばフィールド変数を呼び出せる
- this:今のオブジェクトが持っているフィールド
- this は「インスタンス」を指す参照であり、static メソッドにはインスタンスが存在しない
#### staticフィールド変数
- staticメソッドからthisでは呼び出せない。クラス名.変数名で呼び出す
