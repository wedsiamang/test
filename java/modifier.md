# アクセス修飾子
#### private
- privateメソッド
  privateメソッドは、サブクラスでもアクセスすることはできない。
- privateローカル変数
  privateがついたローカル変数はreturnに使うとコンパイルエラーになる
- 具象クラス、抽象クラス共にprivateメソッドは定義が可能  

#### final
- メソッドにfinalを付与するとオーバーライドを禁止する。
- finalメンバ変数に対して、newインスタンスにつき、１度のみ初期化を行える。インスタンスが別の場合は、それぞれ初期化を行える
